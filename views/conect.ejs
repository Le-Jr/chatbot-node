<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>bem vindo <%= currentUser.name %></title>
  </head>
  <body>
    <div id="client">
        <h1>Bem vindo <%= currentUser.name %>!</h1>
        <% if (currentUser.qrCode) { %>
            <img src="<%= currentUser.qrCode %>" alt="" />
          <% } %>
      <img src="" alt="" />
    </div>
  </body>
</html>